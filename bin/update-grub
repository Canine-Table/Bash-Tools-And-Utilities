function grub() {

    local -i USER_ID="$(id -u)";
    local SUPER_USER="" S="";

    [ "${USER_ID}" -eq 0 ] || for S in 'doas' 'sudo'; do
        command -v "${S}" && {
            SUPER_USER="${S}";
            break;
        }
    done






    return 0;
}
